[gd_resource type="Resource" load_steps=2 format=2]

[ext_resource path="res://src/GameBoard/Difficulty.gd" type="Script" id=1]

[resource]
script = ExtResource( 1 )
name = "Chill"
description = "No queue limit, the game doesn't end when your board is full, and the speed doesn't change"
time_limit = 0.0
queue_size = 10
lose_when_board_full = false
board_size = Vector2( 9, 7 )
speed = 7.0
increase_speed = false
